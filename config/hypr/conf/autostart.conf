#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# Polkit agent - required for privilege escalation dialogs (mount, sudo GUIs, etc.)
exec-once = /usr/lib/polkit-kde-authentication-agent-1

# exec-once = $terminal
# exec-once = nm-applet &
exec-once = waybar 
exec-once = hypridle
exec-once = hyprpaper
exec-once = swaync
#exec-once = caffeine
#exec-once = hyprpanel
